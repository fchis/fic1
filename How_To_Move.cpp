int distanta(int x, int y, int x1, int y1)
{
	return sqrt((x1-x)*(x1-x)+(y1-y)*(y1-y));
}

int i=0;

int main()
{

	//do{---------------------------------------
	if(i%2==0)
	{
		//forward / back
		buff ="f,s"
		d=distanta(x,y,x1,y1);
		//move 
		if moved
			new_d=distanta(x,y,x1,y1);
			
		if(new_d>d)
			//buff = "b,s,b,s"
		else
			//buff = "f,s"
	}
	//	----------------------------------}while( x-x1>10 || x1-x>0 || y-y1>10 || y-y1>10)
	else{
		//left / right
		buff ="l,s"
		d=distanta(x,y,x1,y1);
		//move 
		if moved
			new_d=distanta(x,y,x1,y1);
			
		if(new_d>d)
			//buff = "r,s,r,s"
		else
			//buff = "l,s"
		
	}	


}
